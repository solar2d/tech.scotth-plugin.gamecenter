Sources for the plugin `plugin.gamecenter`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.gamecenter" = {
            publisherId = "tech.scotth",
        },
    },
}
```
